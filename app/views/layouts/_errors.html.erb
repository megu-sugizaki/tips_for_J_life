<!--Indent fixed-->
<% if obj.errors.any? %>
  <div class="flash-message alert">
    <h5><%= pluralize(obj.errors.count, 'errors have occurred') %></h5>
    <% obj.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </div>
<% end %>