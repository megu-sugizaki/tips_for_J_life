<!--Indent fixed-->
<% if obj.errors.any? %>
  <div class="flash-message alert px-2 pt-2">
    <h5><%= pluralize(obj.errors.count, t(".error")) %></h5>
    <% obj.errors.full_messages.each do |message| %>
      <ul><%= message %></ul>
    <% end %>
  </div>
<% end %>